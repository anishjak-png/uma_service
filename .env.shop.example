# Copy to shop PC as .env — run: npm run print-agent
# Must be on same LAN as the printer.

PRINT_AGENT_APP_URL="https://uma-service.vercel.app"
PRINT_AGENT_API_KEY="same-value-as-vercel-PRINT_AGENT_API_KEY"
THERMAL_PRINTER_HOST="192.168.1.87"
THERMAL_PRINTER_PORT="9100"
